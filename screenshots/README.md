# Screenshots

Placeholder directory for architecture / dashboard screenshots.

Suggested additions once the stack has been deployed to AWS:

- `step_functions_graph.png` — Step Functions visual workflow.
- `cloudwatch_dashboard.png` — CloudWatch metrics dashboard.
- `dynamodb_items.png` — Sample items from the hot-lookup table.
- `curated_athena_query.png` — Athena query over the curated Parquet.

These are intentionally omitted from the repo until the pipeline has
been validated against a real AWS account.
